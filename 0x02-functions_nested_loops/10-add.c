#include "main.h"

/**
 * add - Entry point
 *
 * Description: 'function that adds two integers and returns the result'
 *@cat1: first number add
 *@cat2: twice number add
 * Return: result of addition
 */

int add(int cat1, int cat2)
{
	return (cat1 + cat2);
}
